import React from "react";import {PageWrap, ContentWrap} from "../../../components/ComponentsStyles";import CreateForm from "./FormCreate/FormCreate";import SearchBtn from "../../../components/ButtonGroup/SearchBtn";import LeftBar from "../../../components/LeftBar/LeftBar";export default function Construction() {    const [showSearchBtn, setSearchBtn] = React.useState(false)    const handleTabSelected = (index) => {        if (index === 4) {            setSearchBtn(true)        } else setSearchBtn(false)    }    return (        <PageWrap>            <LeftBar>                {showSearchBtn?<SearchBtn/>:null}            </LeftBar>            <ContentWrap>                <CreateForm                    selectedTab={handleTabSelected}                />            </ContentWrap>        </PageWrap>    );}